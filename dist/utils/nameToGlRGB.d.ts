/**
 * Преобразовать цвет из названия/rgb/hsl в массив [R, G, B, alpha] для webGl
 * @param {string} name
 * @returns {[number, number, number, number]} [R, G, B, alpha]
 */
export declare function nameToGlRGB(name: string): [number, number, number, number];
