/**
 * отнять заданное значение от альфа канала цвета
 * @param {string} name цвет
 * @param {number} sub вычитаемое значение (от 0 до 1)
 * @returns {string} hex
 */
export declare function subAlpha(name: string, sub: number): string;
